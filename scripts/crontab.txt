# AWS Credentials
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# MC server
@reboot sleep 15 && cd /mnt/ssd/hom-mc-server && ./scripts/start.sh > /home/pi/hom-mc-server.log 2>&1

# Server backup copy local
0 */6 * * * rsync -r /mnt/ssd/hom-mc-server /mnt/usb/backup/
# 5 * * * * rsync -r /mnt/ssd/hom-mc-server /home/pi/backup/

# Weekly AWS backups
0 3 * * 0 cd /mnt/ssd/hom-mc-server && ./scripts/upload-backup.sh
